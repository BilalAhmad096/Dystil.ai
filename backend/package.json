{
  "name": "dystil-contact-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dystil website form email backend for Cloudflare Workers",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "test": "node --test"
  }
}
